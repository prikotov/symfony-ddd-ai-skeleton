# Test environment — safe defaults for CI and local testing.
# Override in .env.test.local if needed.

APP_ENV=test
APP_DEBUG=1
APP_SECRET=test-secret-not-for-production
APP_DEFAULT_ID=console
DATABASE_URL="sqlite:///:memory:"
