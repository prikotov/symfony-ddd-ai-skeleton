framework:
  messenger:
    default_bus: event.bus
    buses:
      event.bus:
        default_middleware: allow_no_handlers
      command.bus: ~
      query.bus: ~
    transports:
      sync: 'sync://'
